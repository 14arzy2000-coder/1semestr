import random
s = int(input("введите число для 1 кости"))
f = int(input("введите число для 2 кости"))


if (s + f) <= 5:
            print(f"вероятность того, что ваш исход подходит для А: {10 / 36}" )
if s * f <=4:
            print(f"вероятность того, что ваш исход подходит для B: {8 / 36}" )

if (s * f) / 8 ==0:
           print(f"вероятность того, что ваш исход подходит для B: {2/36}")

