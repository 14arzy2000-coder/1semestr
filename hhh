is_running = True
collections = ["task1, task2"]

def show_collection(task_collection):
    print("======")
    for i, j in enumerate(task_collection):
        print(i + 1, j)
    print("======")

print("добро пожаловать")

while is_running:

    choice_user = input("введите свой выбор")
    match str(choice_user):
        case "1":
            show_collection(collections)
            write = input("нажмите энтер")
        case "2":
            add_task = input("введите имя")
            collections.append(add_task)
        case "3":
            show_collection(collections)
            select_edit = int(input("введите номер задачи"))
            edit_name = input("введите новое имя задачи для редактирования")
            if int(select_edit) > 0 and select_edit <= len(collections) :
                collections[select_edit] = collections[select_edit]
                collections[select_edit - 1] = edit_name
                print("задача отредактирована")
            else:
                print("нет такой задачи")
        case "4":
            show_collection(collections)
            delete_task = input("введите новвый номер для удаления")
            if int(select_edit) > 0 and select_edit <= len(collections)):
                    collections.pop(delete_task - 1)
                    print(f"zadacha '{delete_task}' <UNK> <UNK> <UNK>")
            else:
                    print("fghf")
        case "5":
            is_running = False
            print("до свидания")
        case _:
            print("такого пункта нет")
